#!/usr/bin/env node
// import figlet from 'figlet';
// eslint-disable-next-line import/extensions
// eslint-disable-next-line import/extensions

/* figlet('ERROR!', (err, data) => {
  if (err) {
    console.log('Something went wrong...');
    console.dir(err);
    return;
  }
  console.log(data);
}); */

// Poder señalar si la ruta es absoluta o la otra
/* const validPath = (path) => path.absoluteRoute(path);
console.log(validPath('/Users/Yohana Falconi/Desktop/Lab/LIM017-md-links/mdlinks.md'));
*/
